运行 Django 项目教程

前置条件

	•	已安装以下工具：
	•	Python（推荐 3.8 或更高版本）
	•	Git
	•	pip（Python 包管理工具）
	•	如果需要虚拟环境管理，建议安装 venv 或 virtualenv。

步骤 1：克隆仓库

	1.	打开终端。
	2.	使用 git clone 命令将项目拉取到本地：

git clone https://github.com/your-username/your-repo.git

（将 your-username 和 your-repo 替换为实际的 GitHub 用户名和仓库名。）

	3.	进入项目目录：

cd your-repo


	4.	切换到 backend 分支：

git checkout backend

步骤 2：创建虚拟环境

为隔离项目依赖，建议创建虚拟环境：
	1.	创建虚拟环境：

python3 -m venv venv


	2.	激活虚拟环境：
	•	macOS/Linux:

source venv/bin/activate


	•	Windows:

venv\Scripts\activate


	3.	激活后，终端提示符前会显示 (venv)，表示虚拟环境已启用。

步骤 3：安装依赖

使用 requirements.txt 文件安装项目依赖：

pip install -r requirements.txt

步骤 4：设置项目

	1.	迁移数据库（创建表结构）：

python manage.py migrate


	2.	创建超级用户（用于访问 Django 管理后台）：(这个无所谓)

python manage.py createsuperuser

按照提示输入用户名、邮箱和密码。

步骤 5：运行项目

启动 Django 开发服务器：

python manage.py runserver

默认情况下，服务器会在 http://127.0.0.1:8000/ 启动。

步骤 6：访问项目

	1.	打开浏览器/apifox，访问：

http://127.0.0.1:8000/


	2.	如果有 Django 管理后台，访问：

http://127.0.0.1:8000/admin/

使用创建的超级用户账户登录。

注意事项

	1.	依赖安装失败：如果某些依赖安装失败，请检查 Python 和 pip 的版本，或者手动更新 pip：

pip install --upgrade pip


	2.	未激活虚拟环境：每次运行项目前，请确保虚拟环境已激活。
	3.	端口冲突：如果默认端口 8000 被占用，可以指定其他端口：

python manage.py runserver 8080


	4.	跨团队协作：建议团队成员使用相同的 Python 版本和依赖，以减少环境差异问题。

总结

运行 Django 项目大致步骤为：
	1.	克隆仓库：git clone 并切换到 backend 分支。
	2.	创建虚拟环境：python3 -m venv venv。
	3.	安装依赖：pip install -r requirements.txt。
	4.	迁移数据库：python manage.py migrate。
	5.	启动服务器：python manage.py runserver。

此教程适用于开发和测试环境，如需部署到生产环境，还需配置更多内容（如 WSGI、静态文件、数据库等）。